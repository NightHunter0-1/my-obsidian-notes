---
subject: <% tp.file.folder(true) %>
date: <% tp.date.now("YYYY-MM-DD") %>
tags: lecture
---

# Лекция от <% tp.date.now("DD.MM.YYYY") %>

**📚 Предмет:** [[<% tp.file.folder(true) %>]]
**📅 Дата:** <% tp.date.now("DD.MM.YYYY") %>
**🕒 Время:** <% tp.date.now("HH:mm") %>

## 📖 Все лекции этого предмета
```dataview
TABLE date as "Дата создания"
FROM ""
WHERE file.folder = this.file.folder 
AND file.name != this.file.name
AND contains(tags, "lecture")
SORT date DESC
```

## ✏️ Конспект

Здесь пишите конспект лекции...

## 🔗 Связи с другими лекциями
*Все связи автоматически отображаются в списке выше*

- **📍 Текущая папка:** [[<% tp.file.folder(true) %>]]
- **📁 Всего лекций в курсе:** `<%*
const folder = tp.file.folder(true);
const lectures = app.vault.getMarkdownFiles().filter(f => 
    f.path.startsWith(folder + "/") && 
    f.basename != folder
);
-%><%= lectures.length %>`

---
*Автоматически создано <% tp.date.now("DD.MM.YYYY в HH:mm") %>*